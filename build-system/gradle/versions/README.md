# Gradle versions

Gradle の version update 調査を置く。

推奨ファイル名:

```text
gradle-<from>-to-<to>.md
```

例:

```text
gradle-8.10-to-8.11.md
gradle-8.x-to-9.0.md
```

各ファイルは `../../templates/version-diff-template.md` を元に作成する。
