# CI version investigations

CI provider、runner image、JDK、Android SDK / NDK setup、cache、release job などの version update・migration詳細調査を置く。

成果物は `build-system/templates/version-diff-template.md` を基準にし、対応する1ページサマリを `../summaries/` に置く。

まだ詳細調査成果物はない。追加時は `../README.md` とこの索引の両方から参照できるようにする。
